general:
  branches:
    only:
      - feature/circleci

test:
  override:
    - bundle exec kitchen test

deployment:
  aws:
    branch: feature/circelci
    commands:
      - ./packer-build.sh

machine:
  ruby:
    version: '2.2.1'
  services:
    - docker