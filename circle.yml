general:
  branches:
    only:
      - feature/circleci

test:
  override:
    - kitchen test

deployment:
  aws:
    branch: feature/circelci
    commands:
      - ./packer-build.sh

machine:
  ruby:
    version: '2.2.1'